Installation
===================================

To use moval, first install it using pip:

.. code-block:: console

   $ pip install moval


If you are using a jupyter notebook, simply run:

.. code-block:: console

   $ !pip install moval
